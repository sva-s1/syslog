# ── dev / OS noise ──────────────────────────────────────────────
.DS_Store
Thumbs.db
*.sw[op]
.idea/
.vscode/
LOCAL-ONLY/               # scripts only useful on your workstation
*.log                      # ignore **all** log files …

# ── CI fixtures you *do* want tracked ───────────────────────────
!samples/*.log             # … except these three sample logs
!samples/**/*.log

# ── secrets / env files ─────────────────────────────────────────
.env

# ── runtime volumes ─────────────────────────────────────────────
data/
logs/
syslog-ng.persist

# dev-only scripts & scratch files
LOCAL-ONLY/