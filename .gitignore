# ↓ dev noise / local artefacts only
 .DS_Store
 Thumbs.db
 *.sw[op]
 .idea/
 .vscode/
-*.log
+*.log                  # still ignore stray logs…
+!samples/*.log         # …but keep sample fixtures
+!samples/**/*.log      # (covers future sub-folders)
 *.pid
 llm_status.*